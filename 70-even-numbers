public List<Integer> evenNumbers(Integer n) {
    
    List<Integer> ans = new List<Integer>();
    if(n!= null || n>0) 
    {
        for(Integer i=2;i<= n*2;i++)
        {
           if(math.mod(i,2)==0)
           {
               ans.add(i);
           }

        }
        return ans;
    }
}