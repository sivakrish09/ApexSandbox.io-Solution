public List<Integer> fibonacciSeries(Integer n) {
    List<Integer> numList = new List<Integer>();
    for(Integer i =0;i<=n;i++)
    {
        if(i<=1)
        {
            numList.add(1);
        }
        else
        {
            Integer fibo = numList[i]+numList[i-1];
            numList.add(fibo);
        }
    }
    return numList;
}